nacos:
  cmdb:
    dumpTaskInterval: 3600
    eventTaskInterval: 10
    labelTaskInterval: 300
    loadDataAtStart: false

# 数据源相关配置
db:
  num: 1
  user: root
  password: qwer1234
  url:
    0: jdbc:mysql://${MYSQL-HOST:mysql}:${MYSQL-PORT:3306}/${MYSQL-DB:horse_config}?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&nullCatalogMeansCurrent=true

spring:
  datasource:
    platform: mysql
  security:
    enabled: false

server:
  tomcat:
    basedir: logs